
const sayHi = (name) => {
  console.log(`Hello there ${name}`);
};

module.exports = sayHi; // This is the correct way to export a function in Node.js

// when i import a module i need to invoke it

